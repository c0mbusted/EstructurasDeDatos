#include <stdio.h>
#include <stdlib.h>
#define alength 100

//arr with prime numbers only
int main() {
    int arr[100];
    int i;
    int number;

    for(i =2; i<= alength;i++) {
        cont=0;
        if (number%i==0) {
            True
            break;
            cont++;
        }
    }

        if(cont==0 || cont==1 || cont==2) {
            arr[i]=x
            i++;
        }
        printf("%d\n",arrintegers[i]);
    }
        

    return 0;
}

int main(void) {
int x,cont,z,i,tabla[100];
i=0;
 for (x=1;x<=100;x++){
 cont=0;
 for (z=1;z<=x;z++)
 {
 if (x%z==0)
{
 cont++;
 }
 }
 if (cont==2 || z==1 || z==0)
 {
tabla[i]=x;
i++;
 } 
3
}
 for (x=0;x<i;x++)
{
 printf("%d\n",tabla[x]);
}
 system("PAUSE");
return 0;
} 

